#include <stdio.h>
#include "main.h"

/**
 * add - function that adds two numbers
 *
 * Description: function that adds two integers and returns the result
 *
 * @n: the first input pramerter
 *
 * @y: the second input prameter
 *
 * Return: Always 0 (Success)
*/

int add(int n, int y)
{
	int sum = n + y;

	return (sum);
}
