#include <stdio.h>
#include "main.h"

/**
 * print_alphabet_x10 - print the alpha x 10 times
 *
 * Description: function that prints 10 times the alphabet,
 *              in lowercase, followed by a new line.
*/

void print_alphabet_x10(void)
{
	int i, y;

	for (i = 0; i <= 9; i++)
	{
		for (y = 'a'; y <= z; y++)
			_putchar(y);
		_putchar('\n');
	}
}
